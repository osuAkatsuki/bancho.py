apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "banchopy.fullname" . }}-test-connection"
  labels:
    {{- include "banchopy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "banchopy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
